package AST.ExpressionNode;

import AST.ASTNode;
import Utility.Cursor;

public abstract class ExpressionNode extends ASTNode {
    public ExpressionNode(Cursor cursor) {
        super(cursor);
    }
}
